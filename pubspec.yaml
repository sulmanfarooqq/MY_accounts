name: futuristic_portfolio
description: "A next-level interactive web portfolio featuring dynamic cosmic animations."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
dependencies:
  flutter:
    sdk: flutter

  # For creating glowing UI elements.
  flutter_glow: ^0.3.0

  # For social media and project icons.
  font_awesome_flutter: ^10.7.0

  # For custom fonts like Orbitron and Poppins.
  google_fonts: ^6.2.1
  
  # For opening external links (social media, projects).
  url_launcher: ^6.2.6

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices.
  flutter_lints: ^3.0.0

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # No local assets are needed for this version of the code,
  # as the background is now dynamically drawn.
  assets:
    - assets/hero-banner.jpg